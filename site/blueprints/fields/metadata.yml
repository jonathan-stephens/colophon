type: group
fields:
  tags:
    label: Tags
    type: tags
    options: query
    query: site.index.pluck("tags", ",", true)
    required: true
  datetime: fields/growth-status
  datetime: fields/datetime
  location: fields/location
