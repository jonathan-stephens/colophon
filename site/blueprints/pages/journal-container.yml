title: Journal (Container)
type: pages
icon: anchor

tabs:
  entries:
    label: Entries
    sections:
      drafts:
        headline: Draft Entries
        type: pages
        status: draft
        template: journal
        sortBy: date desc

      published:
        headline: Published Entries
        type: pages
        status: published
        template: journal
        sortBy: date desc
        info: "{{ page.date.toDate('Y-m-d') }}"
  extends: tabs/listing
