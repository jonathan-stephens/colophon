<?php

/**
 * Single/Multiple Tag Controller
 *
 * All data is provided by routes/tags.php via $virtualPage->render($templateData).
 * This controller is intentionally empty — the route is the single source of truth.
 *
 * @version 2.0.0
 */

return function ($kirby, $page) {
    return [];
};